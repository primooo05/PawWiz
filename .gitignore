# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Kiro CLI Local Binary
.kiro
kiro-cli.exe

# Env / Infisical — never commit real secrets
.env
.env.*
# ...but keep the committed env template so the project runs without Infisical
!.env.example

# HAR files — contain live tokens, cookies, and session data
*.har
.tokensave
.agents
.codegraph
.rtk

# ObsidianVault
PawWiz-Context/.obsidian/
PawWiz-Context/.trash/
